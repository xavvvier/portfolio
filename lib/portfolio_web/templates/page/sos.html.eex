<p class="sub-title instruction line-9 typewriter-9">## sos ##</p>
<div class="content sos">
   <p><code>SOS</code> is a <a href="https://www.asp.net/mvc">ASP.NET MVC</a> application to manage the occupational health exams carried out by <a href="http://laboratoriocolcan.com/salud-ocupacional/">Laboratorio Colcan</a> to employees of several associated companies.</p>
   <p>The application allows the booking of multiple appointments for a single patient, using <a href="https://www.asp.net/signalr">SignalR</a> to notify in real time the system's users about the assignation preventing the assignation of the same appointment blocks:</p>
   <img src="images/sos-schedule.png" alt="jiralogs login">
   <p>It provides custom controls with the help of <a href="https://getbootstrap.com/">bootstrap</a> to create autocompletion fields that promote user experience:</p>
   <img src="images/sos-autocomplete.png" alt="jiralogs home">
   <p>User administrators have a granular configuration of authorization based on users or groups:</p>
   <img src="images/sos-permissions.png" alt="SOS permissions">
   <p>Between the several configuration options of the application, custom controls such as the Audiogram have been created to allow the input of non-common structured data:</p>
   <iframe height="315" src="https://www.youtube.com/embed/XfLgcyuRcyY" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
</div>
